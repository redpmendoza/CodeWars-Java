public class RemoveFirstAndLastCharacter{
    public static String remove(String str){
    
      // Start of Solution
      String removed = str.substring(1, (str.length()-1));
      return removed; 
      // End of Solution
      
    }
}
